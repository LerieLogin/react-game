import { NavLink, Outlet } from "react-router-dom";

const Navbar = () => {
  return (
    <>
      <nav>
        <NavLink to="/">lkirgweniwgrnoi</NavLink>
      </nav>
      <Outlet />
    </>
  );
};

export default Navbar;
